{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension (WSL build)",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "--folder-uri=file:///C:/Users/mark/apps/test"
      ],
      "outFiles": ["${workspaceFolder}/out/**/*.js"],
      "preLaunchTask": "tsc: watch (WSL root)"
    }
  ]
}
